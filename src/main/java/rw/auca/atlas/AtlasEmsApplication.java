package rw.auca.atlas;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/** Entry point for the Atlas Inventory & Event Resource Management System. */
@SpringBootApplication
public class AtlasEmsApplication {

  public static void main(String[] args) {
    SpringApplication.run(AtlasEmsApplication.class, args);
  }
}
